---
'$schema': 'https://json-schema.org/draft-07/schema'

title: Yakumo YAML Schema
description: Schema for Yakumo YAML files
type: array
items:
  type: object
  properties:
    name:
      type: string
      description: Name of the Yakumo plugin
    config:
      type: object
      description: Configuration for the Yakumo plugin
  required:
    - name
  additionalProperties: true
